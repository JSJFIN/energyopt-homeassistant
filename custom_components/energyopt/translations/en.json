{
  "config": {
    "step": {
      "user": {
        "title": "Connect to EnergyOpt",
        "description": "Enter the details for your EnergyOpt site.",
        "data": {
          "base_url": "API base URL",
          "api_key": "API key",
          "site_id": "Site ID",
          "poll_interval": "Poll interval (seconds)"
        }
      },
      "reconfigure": {
        "title": "Reconfigure EnergyOpt",
        "description": "Update the connection details. Leave the API key blank to keep the current one.",
        "data": {
          "base_url": "API base URL",
          "api_key": "API key",
          "site_id": "Site ID"
        }
      }
    },
    "error": {
      "cannot_connect": "Failed to connect to the EnergyOpt API.",
      "invalid_auth": "Invalid authentication. Check your API key."
    },
    "abort": {
      "already_configured": "This EnergyOpt site is already configured.",
      "reconfigure_successful": "Reconfiguration was successful."
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "EnergyOpt options",
        "description": "Adjust polling and which entities are created.",
        "data": {
          "poll_interval": "Poll interval (seconds)",
          "enable_calendars": "Create schedule calendars"
        }
      }
    }
  }
}
